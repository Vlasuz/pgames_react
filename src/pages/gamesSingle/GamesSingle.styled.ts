import styled from "styled-components";

export const GamesSingleStyled = styled.main`

  .page-game__info {
    display: flex;
    flex-direction: column;
  }

  .page-game__base-info {
    margin-top: auto;
  }
`
