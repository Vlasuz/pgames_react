import styled from "styled-components";

export const HeaderStyled = styled.header`

  .link-to-register {
    height: fit-content;
    cursor: pointer;
    padding-top: 14px;
    padding-bottom: 14px;
    margin-top: auto;
    margin-bottom: auto;
  }

  .header__nav--body {
    //align-items: center;
  }

`
